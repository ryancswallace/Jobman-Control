{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jobman.tech/schema/protocol/agent-acceptance-v1alpha1.schema.json",
  "title": "Jobman agent acceptance v1alpha1",
  "type": "object",
  "additionalProperties": false,
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": {"const": "jobman/v1alpha1"},
    "kind": {"const": "AgentAcceptance"},
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["deliveryId", "executionId", "agentId"],
      "properties": {
        "deliveryId": {"$ref": "#/$defs/id"},
        "executionId": {"$ref": "#/$defs/id"},
        "agentId": {"$ref": "#/$defs/id"}
      }
    },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": ["targetGenerationId", "effectiveExecutionDigest"],
      "properties": {
        "targetGenerationId": {"$ref": "#/$defs/id"},
        "effectiveExecutionDigest": {"$ref": "#/$defs/digest"}
      }
    }
  },
  "$defs": {
    "id": {"type": "string", "format": "uuid"},
    "digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"}
  }
}
