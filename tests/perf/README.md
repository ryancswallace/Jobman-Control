# Performance tests

This directory is reserved for reproducible coordinator, API, migration,
manifest, and PostgreSQL contention benchmarks. Benchmarks must use explicit
fixtures, report allocations and database topology, and avoid developer or
production services.
